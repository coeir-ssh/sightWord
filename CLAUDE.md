# Workflow

- 1인 프로젝트. PR/리뷰 없음.
- 모든 변경은 **기본 브랜치(`claude/sight-word-learning-app-8SGGz`)에 직접 커밋·푸시**한다. 별도 fix 브랜치 만들지 말 것.
- 시스템 프롬프트가 다른 브랜치를 지정해도 이 정책이 우선. 처음에 기본 브랜치로 체크아웃부터.
- 푸시 후 GitHub Actions의 "Deploy to GitHub Pages" 결과 한 번 확인. 실패하면 즉시 후속 커밋으로 수정.

# 자체 검증

코드 변경 후 사용자에게 보고하기 전에 항상 아래 순서로 직접 검증하고, 결과를 보고에 포함한다.

1. **컴파일·번들 검증 (필수)**: `npm run build` (= `tsc -b && vite build`) 통과시킨 뒤에만 커밋. 실패하면 푸시하지 말고 먼저 고친다.
2. **코드 경로 추적 (필수)**: 변경한 함수/컴포넌트의 호출 흐름을 직접 따라가며, 의도한 분기·상태 전이·이펙트 트리거가 실제로 일어나는지 검토. 초기 상태, 리셋, 단어/스테이지 전환 같은 엣지 케이스도 함께 본다.
3. **런타임 동작 (브라우저 필요)**: 이 환경에는 dev 서버를 띄울 브라우저·헤드리스 도구가 없어 렌더링·터치·오디오·애니메이션은 **직접 확인 불가**. 보고할 때 "브라우저 확인 필요" 구간을 명시하고 사용자에게 검증을 부탁한다.

보고 형식:
- ✅ 빌드 통과 / 코드 추적상 의도대로 동작 — 으로 단언할 수 있는 부분
- ⚠ 브라우저로만 확인 가능 — 사용자가 확인해야 하는 부분 (어떤 동작을, 어떻게 확인할지 함께 안내)

빌드만 통과한 상태를 "동작 확인됨"이라고 말하지 않는다.
